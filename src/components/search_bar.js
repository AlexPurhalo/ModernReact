import React from 'react'; // we need React because <input /> still is React.createElement

const SearchBar = () => {
  return <input />
};

export default SearchBar; // To make valid a component in other files